I have added few instruction what you will implement with simple and classic UI. and follow the api guide file /home/rafsun-jani/Desktop/Personal/nestjs_project/my-turborepo/apps/fe/API_GUIDE.md

1. Create a register design. then after register user can see the sign in screen, then user will sign in. after sign in just show a screen where user can see the all users list. with simple header and side nav. then in this user list user can search or filter. user can signout if they want by click. must you will remember when jtw token will be expired then automatically user will be logout.

2. Added another menu name of api-key. which page you will implement api generate, revoke options and copy
3. Craete another one menu name of webhook test, which page you will implement test web hook option where user can paste api-key as header and write body json then test, and user can see the test response success or failed with valid message.
4. Then will craete another menu name of survey response, where all responses will show in a table and user can see with survey name, servey response, survey id,
5. Update the key page, where will keep the genered api key in local storage, so updated api key will show in the ui and user can copy from there, on that time don't show the generate new key if exist api key, I have added end point in api guide file.

First of all craete the better folder structure.
